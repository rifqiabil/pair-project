<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Car Lists</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
    <style>
      /* Optional: Custom styles for better alignment */
      body {
        margin: 20px;
      }
      .card {
        margin-bottom: 20px; /* Added margin to create space between cards */
      }
      table {
        width: 100%;
      }
    </style>
  </head>
  <body>
    <%- include('../partials/_navbar') %>

    <div class="container mt-4">
      <h1 class="text-center mb-4 mt-5">Car Lists</h1>

      <div class="row">
        <% data.forEach(el => { %>
        <div class="col-md-4 mb-4">
          <div class="card">
            <img src="<%= el.carImage %>" class="card-img-top" alt="Car Picture" style="height: 200px; object-fit: cover" />
            <div class="card-body">
              <h5 class="card-title"><%= el.title %></h5>
              <p class="card-text"><span style="font-weight: bold">Transmission:</span> <%= el.transmission %></p>
              <p class="card-text"><span style="font-weight: bold">Milage:</span> <%= numberWithCommas(el.milage) %> km</p>
              <p class="card-text"><span style="font-weight: bold">Price:</span> <%= toRupiah(el.price) %></p>
              <p class="card-text"><span style="font-weight: bold">Description:</span> <%= el.description %></p>
              <div class="text-center">
                <a href="#" class="btn btn-primary">Buy</a>
                <% if (role === "admin") { %>
                <a href="#" class="btn btn-warning">Edit</a>
                <a href="#" class="btn btn-danger">Delete</a>
                <% } %>
              </div>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  </body>
</html>
